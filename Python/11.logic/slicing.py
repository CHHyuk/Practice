# slicing : 리스트나 문자열에서 값을 여러개 가져오는 기능

list = [1,2,3,4,5]
print(list[1]) # 2

text = 'CHANG HAN HYUK'
print(text[0:14]) # index 0부터 14까지 출력
print(text[0:]) #index 0부터 끝까지 출력
print(text[:10]) #index 처음부터 10까지 출력
print(text[:]) #처음부터 끝까지 출력